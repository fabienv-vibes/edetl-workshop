resources:
  schemas:
    pipeline_schema:
      catalog_name: ${var.catalog}
      name: ${var.schema}
      comment: "edetl-workshop output tables (bronze/silver/gold)"

  volumes:
    raw_landing:
      catalog_name: ${var.catalog}
      # Reference the schema resource directly so dev-mode prefixes resolve consistently.
      schema_name: ${resources.schemas.pipeline_schema.name}
      name: ${var.volume}
      volume_type: MANAGED
      comment: "Auto Loader raw JSON landing zone for the edetl-workshop pipeline"
